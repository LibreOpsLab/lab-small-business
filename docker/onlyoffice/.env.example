# Copy to .env and fill in real values. Never commit the real .env (see .gitignore).
# Must match ONLYOFFICE_JWT_SECRET in docker/nextcloud/.env so NextCloud's connector app
# and the Document Server agree on request signing.

ONLYOFFICE_JWT_SECRET=changeme-generate-with-openssl-rand
